version: '3.8'

services:
  atlanta-net:
    build: .
    container_name: atlanta-net-inference
    image: atlanta-net:latest

    # environment:
    #   - PYTHONUNBUFFERED=1
    #   - PYTHONPATH=/app

    working_dir: /app

    command: ["bash", "entrypoint.sh"]
    #command: ["ls", "-la"]
